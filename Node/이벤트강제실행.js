process.on("exit", () => console.log("장비를 종료합니다."));

process.emit("exit"); //종료 이벤트를 강제 실행
process.emit("exit"); //종료 이벤트를 강제 실행
process.emit("exit"); //종료 이벤트를 강제 실행
process.exit(0);
process.emit("exit"); //종료 이벤트를 강제 실행
process.emit("exit"); //종료 이벤트를 강제 실행
process.emit("exit"); //종료 이벤트를 강제 실행
process.emit("exit"); //종료 이벤트를 강제 실행
